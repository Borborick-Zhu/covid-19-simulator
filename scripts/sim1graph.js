var sim1graph = function(p) {
    const FRAME_RATE = 10;
    
    p.setup = function() {
        p.createCanvas(1100, 400);
        p.background(0);
        p.frameRate(FRAME_RATE);
    };

    function createLineDrawer(numberOfBalls) {
        
        var lineDrawer = new Object();
        
    }

}